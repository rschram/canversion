{# templates/lecture_scripts/printable_script.md.j2 #}
---
title: "{{ lecture_title | default('Lecture Script') }}"
subtitle: "Week {{ week_number | default('N/A') }} - {{ week_title | default('N/A') }}"
author: "{{ class_details.teaching_staff.coordinator.name | default(class_details.user_details.name | default('')) }}"
date: "{{ lecture_date | default('today') }}" # Pandoc can interpret 'today'
# Add any other metadata Pandoc can use for PDF/DOCX, e.g., abstract, keywords
# For PDF via LaTeX, you can specify geometry, font, etc. here or via a LaTeX template.
# geometry: "left=1in,right=1in,top=1in,bottom=1in" 
# fontfamily: "sfdefault" # Example: sans-serif
# fontsize: 11pt
---

{# Optional: A more structured title page if not relying purely on Pandoc's default #}
{# Or leave it to Pandoc and the reference.docx / LaTeX template for styling #}

{{ lecture_script_content_md }}

{# If you want bibliography at the end and it's not auto-generated by Pandoc's --citeproc in the main body #}
{% if bibliography_path %}
## References
{# Pandoc will replace this section if --citeproc is used and references are cited #}
{% endif %}